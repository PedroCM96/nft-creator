import styled from "styled-components";
import {theme} from "./theme";

export const CssAppWrapper = styled.div`
background-color: ${theme.palette.bg.primary};
  display: flex;
  justify-content: center;
  width: 100%;
  height: 100vh;
`